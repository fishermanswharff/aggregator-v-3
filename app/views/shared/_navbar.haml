:ruby
  is_logged_in = current_user.present?

%nav.top-bar
  .top-bar-left
    %ul.menu
      %li.menu-text Aggregator
      %li= link_to 'Home', root_path
  - if is_logged_in
    .top-bar-right
      %ul.menu
        %li= link_to 'Logout', logout_path
        %li= link_to 'Account', user_path(current_user)
